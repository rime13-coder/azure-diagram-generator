# Azure Configuration
# Comma-separated subscription IDs, or "all" to discover all accessible subscriptions
AZURE_SUBSCRIPTION_IDS=all

# Lucidchart API Configuration
# Obtain from: https://developer.lucidchart.com/
LUCIDCHART_API_KEY=

# Output Configuration
# Default output format: lucidchart, drawio, mermaid
DEFAULT_OUTPUT_FORMAT=lucidchart
# Default diagram type: high-level, network, app, dataflow, all
DEFAULT_DIAGRAM_TYPE=all
# Default output directory for generated diagrams
OUTPUT_DIR=./output

# Project name (used to label generated diagrams)
PROJECT_NAME=my-azure-project

# Resource Filters (optional)
# Comma-separated resource group names to include
# INCLUDE_RESOURCE_GROUPS=rg-prod,rg-staging
# Comma-separated resource types to exclude (full Azure type string)
# EXCLUDE_RESOURCE_TYPES=microsoft.insights/components,microsoft.operationalinsights/workspaces
# Filter by tag: key=value
# FILTER_TAG=environment=production
